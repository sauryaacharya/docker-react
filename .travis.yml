sudo: required
services:
  - docker

before_install:
  - docker build -t sauryaacharya/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true sauryaacharya/docker-react yarn test --coverage
